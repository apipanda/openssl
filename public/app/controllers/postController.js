app.controller("PostController", [
    "$scope",
    "$location",
    "$log",
    function ($scope, $location, $log) {
        'use strict';

        $log.debug("Post Controller Initialized");
    }]);
