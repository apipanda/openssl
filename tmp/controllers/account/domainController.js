app.controller("DomainController", [
    "$scope",
    "$location",
    "$log",
    function ($scope, $location, $log) {
        'use strict';

        $log.debug("Domain Controller Initialized");
    }]);
