machine:
  timezone:
    Africa/Lagos
  environment:
    ENV: TEST

dependencies:
  pre:
    - pip install coveralls
  override:
    - pip install -r requirements/test.txt


test:
  override:
    - coverage run manage.py test
